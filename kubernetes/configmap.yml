apiVersion: v1
kind: ConfigMap
metadata:
  name: taskmanager-config
  namespace: task-manager
data:
  PORT: "3001"
  NODE_ENV: "production"
  JWT_EXPIRY: "1h"
  REFRESH_TOKEN_TTL: "7d"
  BCRYPT_SALT_ROUNDS: "12"
  CORS_ORIGIN: "https://taskmanager.example.com"
